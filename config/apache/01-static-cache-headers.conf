# Far-future caching headers for static assets.
# Browsers will cache these locally and skip network requests until expiry.
#
# mod_expires is already loaded in the WordPress image.
# mod_headers needs to be loaded from the Debian module path.
LoadModule headers_module /usr/lib/apache2/modules/mod_headers.so

<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresDefault "access plus 1 month"

    # CSS & JavaScript
    ExpiresByType text/css                  "access plus 1 year"
    ExpiresByType text/javascript            "access plus 1 year"
    ExpiresByType application/javascript     "access plus 1 year"
    ExpiresByType application/x-javascript   "access plus 1 year"

    # Images
    ExpiresByType image/jpeg                 "access plus 1 year"
    ExpiresByType image/png                  "access plus 1 year"
    ExpiresByType image/gif                  "access plus 1 year"
    ExpiresByType image/webp                 "access plus 1 year"
    ExpiresByType image/avif                 "access plus 1 year"
    ExpiresByType image/svg+xml              "access plus 1 year"
    ExpiresByType image/x-icon               "access plus 1 year"

    # Fonts
    ExpiresByType font/woff                  "access plus 1 year"
    ExpiresByType font/woff2                 "access plus 1 year"
    ExpiresByType application/font-woff      "access plus 1 year"
    ExpiresByType application/font-woff2     "access plus 1 year"
    ExpiresByType font/ttf                   "access plus 1 year"
    ExpiresByType font/otf                   "access plus 1 year"
    ExpiresByType application/vnd.ms-fontobject "access plus 1 year"
</IfModule>

<IfModule mod_headers.c>
    <FilesMatch "\.(css|js|jpg|jpeg|png|gif|webp|avif|svg|ico|woff|woff2|ttf|otf|eot)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>

    <FilesMatch "\.(html|htm|php)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>
</IfModule>

<IfModule mod_headers.c>
    <If "%{THE_REQUEST} =~ /sitemap.*\.xml/">
        Header set Cache-Control "public, max-age=3600, s-maxage=3600"
        Header unset Pragma
        Header unset Expires
    </If>
    <If "%{THE_REQUEST} =~ m#/robots\.txt#">
        Header set Content-Type "text/plain; charset=UTF-8"
    </If>
</IfModule>
